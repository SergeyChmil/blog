import { PhotosNumberPipe } from './photos-number.pipe';

describe('PhotosNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new PhotosNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
