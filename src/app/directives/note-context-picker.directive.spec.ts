import { NoteContextPickerDirective } from './note-context-picker.directive';

describe('NoteContextPickerDirective', () => {
  it('should create an instance', () => {
    const directive = new NoteContextPickerDirective();
    expect(directive).toBeTruthy();
  });
});
