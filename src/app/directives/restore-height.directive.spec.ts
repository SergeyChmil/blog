import { RestoreHeightDirective } from './restore-height.directive';

describe('RestoreHeightDirective', () => {
  it('should create an instance', () => {
    const directive = new RestoreHeightDirective();
    expect(directive).toBeTruthy();
  });
});
